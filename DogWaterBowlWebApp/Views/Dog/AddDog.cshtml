@model Dog

<h2>Add a New Dog</h2>

@using (Html.BeginForm("AddDogToDatabase", "Dog", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <input asp-for="DogID" type="hidden" value="@Model.DogID" />

    <label asp-for="Name" class="control-label">What is your dog's name?</label>
    <input type="text" name="Name" class="form-control" value="@Model.Name" required />

    <label asp-for="Amount" class="control-label">How much water do you want to dispense? (In cups)</label>
    <input type="number" step="0.01" name="Amount" value="@Model.Amount cups" />

    <label asp-for="TimeInterval" class="control-label">How long do you want to wait until that amount is allowed to be dispensed again? (In minutes)</label>
    <input type="number" step="1" name="TimeInterval" value="@Model.TimeInterval minutes" />

    <div class="row">
        <label asp-for="Picture" class="control-label">Choose a profile picture for your dog!</label>
        <div class="form-group col-md-6">
            <input type="file" class="form-control" name="Picture" accept="image/*" value="@Model.Picture"/>
        </div>
    </div>

    <input type="submit" value="Add Dog" class="btn btn-primary" />
}